'use client';

import HeroDemo from "@/components/ui/hero-demo";

const About = () => {
  return (
    <div id="about" className="w-full bg-background pt-10">
      <HeroDemo />
    </div>
  );
};

export default About;
